# Smart Trip 项目架构目录说明

本文档说明 `/home/obiah/Desktop/smart-trip` 当前项目结构、后端重构后的分层设计、各目录职责和关键文件用途。后端已经从原先 `app/` 下大量技术目录平铺，调整为更清晰的工程分层：`api / core / orchestration / knowledge / capabilities`。

## 1. 总体结构

```text
smart-trip/
├── backend/                 后端 FastAPI 服务、规划链路、RAG、Memory、Skill、Agent、数据与测试
├── frontend/                Vue 3 + Vite 前端，包含用户视图和开发调试视图
├── skills/                  可被后端调度的业务 Skill，每个 Skill 包含 SKILL.md 和执行脚本
├── deploy/                  Nginx、FRP 和部署辅助配置
├── README.md                项目简介、核心能力、技术栈和启动方式
├── docker-compose.yml       容器化编排配置
├── .env                     根目录本地环境变量，通常用于部署或 compose
├── .env.production.example  生产环境变量示例
├── .dockerignore            Docker 构建忽略规则
├── .gitignore               Git 忽略规则
└── 项目架构目录说明.md       当前文档
```

## 2. 根目录文件

| 文件 | 说明 |
|---|---|
| `README.md` | 项目简要介绍、核心能力、技术栈、启动命令和配置说明。 |
| `docker-compose.yml` | 定义 `backend` 和 `web` 两个容器服务，后端挂载 `backend/data`，前端通过 Nginx 对外提供服务。 |
| `.env` | 根目录本地环境变量，可能包含运行配置；真实密钥不应提交。 |
| `.env.production.example` | 生产环境变量模板。 |
| `.dockerignore` | Docker 构建忽略规则。 |
| `.gitignore` | Git 忽略规则。 |
| `sandbox-write-should-fail.txt` | 沙箱写入测试产生的验证文件，不属于主业务逻辑。 |

## 3. 后端目录 `backend/`

```text
backend/
├── app/
├── data/
├── scripts/
├── tests/
├── Dockerfile
├── pyproject.toml
├── .env
└── .env.example
```

| 文件/目录 | 说明 |
|---|---|
| `backend/app/` | 后端应用源码。 |
| `backend/data/` | 运行数据、SQLite、RAG 索引、mock 数据、地图缓存、图片资产等。 |
| `backend/scripts/` | 数据构建、RAG 构建、图片任务和资产审计脚本。 |
| `backend/tests/` | 单元测试和集成测试。 |
| `backend/Dockerfile` | 后端镜像构建文件。 |
| `backend/pyproject.toml` | Python 项目依赖和 pytest 配置。 |
| `backend/.env` | 后端本地环境变量，包括模型、RAG、Embedding、地图和图片生成配置。 |
| `backend/.env.example` | 后端环境变量示例。 |

## 4. 后端源码分层 `backend/app/`

```text
backend/app/
├── main.py
├── scripts.py
├── api/
├── core/
├── orchestration/
├── knowledge/
└── capabilities/
```

分层原则：

| 层级 | 职责 |
|---|---|
| `api/` | HTTP 接入层，只负责请求响应适配。 |
| `core/` | 系统基础设施，包括配置、路径、数据库、schema、静态媒体。 |
| `orchestration/` | 业务编排层，包括旅行规划主链路、Workflow、Agent、Context、Memory。 |
| `knowledge/` | 知识层，包括 RAG、Retrieval 和知识图谱。 |
| `capabilities/` | 能力层，包括 Skill/Tool/Sandbox、LLM、地图、图片生成、外部集成和本地数据引擎。 |

这样调整后，`app/` 顶层不再堆十几个技术目录，核心边界更接近软件系统开发中的分层架构。

## 5. 应用入口

| 文件 | 说明 |
|---|---|
| `backend/app/main.py` | FastAPI 应用入口。负责初始化 SQLite、加载配置、装配 Graph、Skill、LLM、Memory、RAG、地图、图片生成和 PlannerService，并注册 API 路由。 |
| `backend/app/scripts.py` | 后端启动辅助逻辑。负责确保本地 RAG Markdown、chunk manifest、BM25 索引和可选向量索引可用。 |
| `backend/app/__init__.py` | 应用包初始化文件。 |

## 6. API 层 `backend/app/api/`

```text
backend/app/api/
├── __init__.py
└── routes.py
```

| 文件 | 说明 |
|---|---|
| `routes.py` | FastAPI 路由定义。包含规划请求、健康检查、会话历史、Skill 列表与调用、图片生成、地图展示等接口。 |
| `__init__.py` | API 包初始化文件。 |

API 层不承载复杂业务，只把 HTTP 请求转给 `PlannerService`、`ToolService`、`MemoryService`、`ImageGenerationService` 等服务。

## 7. Core 基础层 `backend/app/core/`

```text
backend/app/core/
├── paths.py
├── config/
├── db/
├── models/
└── media/
```

| 文件/目录 | 说明 |
|---|---|
| `paths.py` | 统一路径工具，提供 `app_root()`、`backend_root()`、`project_root()`、`data_dir()`，避免业务代码依赖 `parents[n]`。 |
| `config/settings.py` | 统一读取环境变量并构造 `Settings`。包含主模型、图片生成、高德地图、RAG、Embedding、知识图谱、CORS 等配置。 |
| `config/__init__.py` | 配置包初始化文件。 |
| `db/sqlite.py` | SQLite 初始化、连接、表结构创建和 JSON 快照兼容。 |
| `db/repositories.py` | Repository 层，封装 Memory 和 Session Run 的读写。优先 SQLite，兼容 JSON fallback。 |
| `db/__init__.py` | 数据库包初始化文件。 |
| `models/schemas.py` | Pydantic API schema，定义规划请求、响应、Skill、图片生成、会话历史等数据结构。 |
| `models/__init__.py` | 模型包初始化文件。 |
| `media/static_files.py` | 静态资源服务扩展，为生成图片设置浏览器缓存头。 |
| `media/__init__.py` | 媒体包初始化文件。 |

## 8. Orchestration 编排层 `backend/app/orchestration/`

```text
backend/app/orchestration/
├── planning/
├── workflow/
├── agents/
├── context/
└── memory/
```

该层负责“用户输入之后如何完成一次旅行任务”，是业务主链路所在地。

### Planning

| 文件 | 说明 |
|---|---|
| `planning/planner_service.py` | 旅行规划核心服务。串联任务路由、槽位抽取、会话恢复、Memory、RAG、Skill、Agent、结果生成和持久化。 |
| `planning/task_router.py` | 任务路由。判断简单咨询、天气问答、景点推荐、复杂规划、追问重规划和澄清请求。 |
| `planning/slot_extractor.py` | 槽位抽取。抽取目的地、天数、预算、节奏、住宿偏好、餐饮偏好和排除景点。 |
| `planning/revision_intent.py` | 追问/修改意图识别。判断用户是否在上一轮方案基础上调整预算、住宿、路线或排除景点。 |
| `planning/__init__.py` | Planning 包初始化文件。 |

### Workflow

| 文件 | 说明 |
|---|---|
| `workflow/graph.py` | 工作流图定义，把输入处理、记忆加载、RAG、Skill、Agent、Review、响应生成组织成可追踪流程。 |
| `workflow/state.py` | 工作流状态类型，描述一次请求在各阶段流转的数据。 |
| `workflow/trace.py` | 阶段追踪工具，记录节点状态、摘要和元信息。 |
| `workflow/reviewer.py` | 最终方案 Reviewer，检查排除景点、天数、预算、节奏等硬约束，并支持局部修复。 |
| `workflow/__init__.py` | Workflow 包初始化文件。 |

### Workflow Nodes

| 文件 | 说明 |
|---|---|
| `workflow/nodes/input_nodes.py` | 输入阶段：会话 ID、用户 ID、任务画像、槽位抽取、会话恢复。 |
| `workflow/nodes/context_nodes.py` | 上下文阶段：Memory 加载、RAG 检索、Context 组装。 |
| `workflow/nodes/execution_nodes.py` | 执行阶段：Skill 选择与执行、多 Agent 执行、Reviewer 校验和修复。 |
| `workflow/nodes/agent_nodes.py` | Agent 阶段封装。 |
| `workflow/nodes/response_nodes.py` | 响应阶段：咨询响应、完整规划响应、地图/图片增强、LLM 摘要、Memory 写回、会话保存。 |
| `workflow/nodes/__init__.py` | 节点包初始化文件。 |

### Agents

| 文件 | 说明 |
|---|---|
| `agents/base.py` | Agent 基础类型和通用输出结构。 |
| `agents/agent_service.py` | 多 Agent 协作服务入口。 |
| `agents/planner_agent.py` | Planner Agent，负责需求拆解和任务规划。 |
| `agents/retriever_agent.py` | Retriever Agent，负责整理 RAG 和工具检索结果。 |
| `agents/executor_agent.py` | Executor Agent，负责生成结构化旅行方案。 |
| `agents/reviewer_agent.py` | Reviewer Agent，负责一致性和约束检查。 |
| `agents/__init__.py` | Agent 包初始化文件。 |

当前 Agent 主要是确定性阶段拆分和工程化协作，不是每个 Agent 都独立发起大模型会话。

### Context

| 文件 | 说明 |
|---|---|
| `context/context_assembler.py` | Context 组装器。整合系统角色、当前请求、结构化约束、Memory、RAG、Skill 结果、Agent 输出。 |
| `context/session_context_service.py` | 会话上下文服务。基于历史运行记录恢复当前会话的基线约束和主规划结果。 |
| `context/__init__.py` | Context 包初始化文件。 |

### Memory

| 文件 | 说明 |
|---|---|
| `memory/memory_service.py` | Memory 读写服务，负责长期偏好保存、读取和更新。 |
| `memory/memory_injection_service.py` | Memory 注入服务，按当前任务筛选相关记忆并生成结构化摘要。 |
| `memory/memory_extractor.py` | Memory 抽取逻辑，识别用户表达中适合沉淀为长期记忆的偏好。 |
| `memory/preference_profile.py` | 偏好 Profile 定义和归一化逻辑。 |
| `memory/__init__.py` | Memory 包初始化文件。 |

## 9. Knowledge 知识层 `backend/app/knowledge/`

```text
backend/app/knowledge/
├── rag/
├── retrieval/
└── graph/
```

该层负责“系统知道什么”和“如何检索知识”。

### RAG

| 文件 | 说明 |
|---|---|
| `rag/types.py` | RAG 数据类型，例如 `KnowledgeChunk`。 |
| `rag/tokenizer.py` | 中文/英文混合 token 切分，用于 BM25 和关键词统计。 |
| `rag/corpus_builder.py` | Markdown 语料解析和 chunk 构建。解析 frontmatter，按标题层级切片并生成 metadata。 |
| `rag/corpus_loader.py` | 从 manifest 或索引加载知识 chunk。 |
| `rag/indexer.py` | BM25 索引构建器，生成 `rag_index.json`。 |
| `rag/embedding_client.py` | OpenAI-compatible Embedding 客户端。 |
| `rag/vector_index.py` | 本地向量索引构建和读取，生成/消费 `embedding_index.json`。 |
| `rag/retriever.py` | 本地 RAG 检索器，完成城市过滤、BM25、向量相似度、hybrid score、业务重排和 topic 去重。 |
| `rag/__init__.py` | RAG 包初始化文件。 |

### Retrieval

| 文件 | 说明 |
|---|---|
| `retrieval/query_rewrite.py` | 规则型 query rewrite，把目的地、天数、预算、节奏、偏好和原话改写成检索词。 |
| `retrieval/retrieval_service.py` | RAG 服务门面。确保索引可用，调用 LocalRAGRetriever，返回检索步骤、signals、命中文档和注入知识。 |
| `retrieval/__init__.py` | Retrieval 包初始化文件。 |

### Graph

| 文件 | 说明 |
|---|---|
| `graph/loader.py` | 从 Obsidian Markdown vault 加载节点和边。 |
| `graph/store.py` | SQLite 图谱存储，维护 `graph_nodes` 和 `graph_edges`。 |
| `graph/service.py` | 图谱扩展服务，用于景点别名、关联点位或排除项扩展。 |
| `graph/__init__.py` | Graph 包初始化文件。 |

这里的 Graph 是项目自定义知识图谱能力，不等同于 LangGraph。

## 10. Capabilities 能力层 `backend/app/capabilities/`

```text
backend/app/capabilities/
├── skills/
├── llm/
├── integrations/
├── image_generation/
├── presentation/
└── mock/
```

该层负责“系统能调用什么能力”，包括模型、工具、沙箱、外部服务和本地业务能力。

### Skills / Tools / Sandbox

| 文件 | 说明 |
|---|---|
| `skills/skill_registry.py` | Skill 注册表。扫描 `skills/public/*/SKILL.md`，解析 frontmatter 和 `When to Use` 示例。 |
| `skills/skill_selection_service.py` | Skill 选择服务。结合 fast path、规则启发式和 LLM 选择，决定本轮需要哪些 Skill，并补全依赖。 |
| `skills/tool_service.py` | Tool 执行门面。根据 input schema 构造 payload，调用 Skill，并包装成统一 tool_result。 |
| `skills/skill_script_runner.py` | Skill 脚本执行门面。当前所有脚本都交给 Sandbox Runner。 |
| `skills/skill_sandbox.py` | Skill 沙箱执行器。以受限子进程运行脚本，限制超时、内存、CPU、文件句柄、网络、子进程和写入目录，并解析 stdout JSON。 |
| `skills/__init__.py` | Skills 包初始化文件。 |

### LLM

| 文件 | 说明 |
|---|---|
| `llm/openai_client.py` | OpenAI-compatible Chat Completions 客户端。当前用于 Skill 选择和最终自然语言摘要生成，支持 fallback 摘要。 |
| `llm/__init__.py` | LLM 包初始化文件。 |

### Integrations

| 文件 | 说明 |
|---|---|
| `integrations/amap_geo_service.py` | 高德地图 Web Service 集成，用于地理编码、POI 坐标和路线展示。 |
| `integrations/mcp_gateway.py` | MCP-style 网关占位实现。当前为本地 MockMCPGateway，未来可替换成真实 MCP Client。 |
| `integrations/__init__.py` | Integrations 包初始化文件。 |

### Image Generation

| 文件 | 说明 |
|---|---|
| `image_generation/client.py` | OpenAI-compatible 图片生成客户端。 |
| `image_generation/service.py` | 图片生成服务主入口，负责生成、缓存和挂载图片资产。 |
| `image_generation/prompt_builder.py` | 图片生成 Prompt 构建器。 |
| `image_generation/task_builder.py` | 图片批量生成任务构建器。 |
| `image_generation/asset_utils.py` | 图片资产路径、文件名、缓存 key、URL 处理工具。 |
| `image_generation/thumbnails.py` | 缩略图生成逻辑。 |
| `image_generation/audit.py` | 图片资产审计逻辑。 |
| `image_generation/__init__.py` | 图片生成包初始化文件。 |

### Presentation

| 文件 | 说明 |
|---|---|
| `presentation/geo_service.py` | 地图展示数据组装服务，为最终方案补充城市、POI 和路线展示数据。 |
| `presentation/__init__.py` | Presentation 包初始化文件。 |

### Mock Engine

| 文件 | 说明 |
|---|---|
| `mock/travel_engine.py` | 本地旅行数据处理引擎，提供天气、景点筛选、酒店筛选、路线规划等确定性函数。 |
| `mock/__init__.py` | Mock 包初始化文件。 |

## 11. 后端数据目录 `backend/data/`

```text
backend/data/
├── smart_trip.sqlite3
├── memory_store.json
├── session_runs_store.json
├── knowledge/
├── mock/
├── rag/
├── presentation/
├── knowledge_graph/
└── generated_images/
```

| 文件/目录 | 说明 |
|---|---|
| `smart_trip.sqlite3` | SQLite 主数据库，保存 Memory、Session Runs、知识图谱等。 |
| `memory_store.json` | Memory JSON 兼容/回退存储。 |
| `session_runs_store.json` | 会话运行记录 JSON 兼容/回退存储。 |
| `knowledge/cities.json` | 城市基础知识数据。 |
| `mock/travel_data.json` | 本地旅行业务数据，供 Skill 和规则引擎使用。 |
| `rag/raw_markdown/*.md` | 城市 Markdown RAG 原始语料。 |
| `rag/corpus/manifest.json` | Markdown 切片后的 chunk manifest。 |
| `rag/index/rag_index.json` | BM25 检索索引。 |
| `rag/index/embedding_index.json` | 本地 chunk embedding 向量索引。 |
| `presentation/*.json` | 城市/POI 坐标、高德缓存和路线展示缓存。 |
| `knowledge_graph/` | 图谱快照和 Obsidian vault。 |
| `generated_images/` | 生成图片、缩略图、manifest 和图片任务报告。 |

## 12. 后端脚本 `backend/scripts/`

| 文件 | 说明 |
|---|---|
| `seed_mock_data.py` | 生成或补充本地旅行 mock 数据。 |
| `build_rag_markdown_corpus.py` | 从结构化数据生成城市 Markdown RAG 语料。 |
| `build_rag_corpus.py` | 构建 RAG manifest 和 BM25 索引。 |
| `build_rag_vector_index.py` | 调用 Embedding 服务构建本地向量索引。 |
| `build_trip_visual_tasks.py` | 生成旅行图片批量任务。 |
| `generate_local_trip_images.py` | 执行图片生成任务。 |
| `generate_trip_image_thumbnails.py` | 生成缩略图。 |
| `audit_trip_visual_assets.py` | 审计图片资产是否完整可用。 |

## 13. 后端测试 `backend/tests/`

```text
backend/tests/
├── unit/
└── integration/
```

| 目录 | 说明 |
|---|---|
| `tests/unit/` | 单元测试，覆盖 Agent、RAG、Embedding、Memory、Skill、Slot、SQLite、Tool、Workflow Reviewer、图片资产等。 |
| `tests/integration/` | API 集成测试和 smoke test，验证从 HTTP 请求到规划响应的端到端链路。 |

当前重构后已验证：

```text
105 passed, 35 deselected
```

## 14. 前端目录 `frontend/`

```text
frontend/
├── src/
├── dist/
├── node_modules/
├── package.json
├── package-lock.json
├── vite.config.js
├── index.html
├── Dockerfile
└── .env
```

| 文件/目录 | 说明 |
|---|---|
| `package.json` | Vue 3 + Vite 前端配置，提供 `npm run dev` 和 `npm run build`。 |
| `package-lock.json` | npm 依赖锁定。 |
| `vite.config.js` | Vite 配置。 |
| `index.html` | 前端 HTML 入口。 |
| `Dockerfile` | 前端镜像构建文件。 |
| `.env` | 前端本地环境变量。 |
| `dist/` | 构建产物，可重新生成。 |
| `node_modules/` | 依赖目录，可重新安装。 |

## 15. 前端源码 `frontend/src/`

```text
frontend/src/
├── App.vue
├── main.js
├── styles.css
├── components/
└── lib/
```

| 文件 | 说明 |
|---|---|
| `main.js` | Vue 应用入口。 |
| `App.vue` | 前端主页面，负责用户视图/开发调试视图切换、请求状态、会话状态、结果展示和追问输入。 |
| `styles.css` | 全局样式，包含用户视图、调试视图、结果卡片、地图图片、浮动追问栏等。 |

### 组件 `frontend/src/components/`

| 文件 | 说明 |
|---|---|
| `ResultPanel.vue` | 用户视图结果展示核心组件。 |
| `AmapTripMapPanel.vue` | 高德地图展示组件。 |
| `TripMapPanel.vue` | 本地/轻量地图预览组件。 |
| `RequestForm.vue` | 旅行需求输入表单。 |
| `SessionHistoryPanel.vue` | 会话历史和最近结果展示。 |
| `DevResponsePanel.vue` | 开发调试 Runtime Snapshot。 |
| `PipelinePanel.vue` | 开发调试 Stage Trace。 |
| `ContextPanel.vue` | Prompt Context 展示。 |
| `MemoryPanel.vue` | Memory 调试展示。 |
| `SkillsPanel.vue` | Skills 调试展示。 |
| `StructuredStatePanel.vue` | 结构化槽位展示。 |
| `StateMaintenancePanel.vue` | 状态维护/调试组件。 |

### 工具 `frontend/src/lib/`

| 文件 | 说明 |
|---|---|
| `api.js` | 前端 API 客户端。 |
| `amap.js` | 高德地图 JS SDK 加载工具。 |
| `planExport.js` | 行程方案导出工具。 |

当前重构后已验证：

```text
npm run build
✓ built
```

## 16. Skill 目录 `skills/`

```text
skills/public/
├── attraction-search/
├── weather-lookup/
├── hotel-search/
├── route-plan/
├── budget-optimize/
├── itinerary-audit/
├── knowledge-snapshot/
├── sandbox-timeout-test/
└── sandbox-write-test/
```

每个 Skill 目录包含：

```text
SKILL.md
scripts/run.py
```

| Skill | 说明 |
|---|---|
| `attraction-search` | 景点搜索，根据目的地、偏好、节奏、天数和排除项筛选候选景点。 |
| `weather-lookup` | 天气查询和出行适宜性判断。 |
| `hotel-search` | 酒店推荐，根据预算和住宿偏好筛选住宿候选。 |
| `route-plan` | 路线规划，把候选景点分配成按天路线，支持避开指定景点。 |
| `budget-optimize` | 预算拆分和住宿/体验升级建议。 |
| `itinerary-audit` | 行程审计，检查天数、排除景点、预算、住宿、天气等一致性。 |
| `knowledge-snapshot` | 开发调试类能力，返回知识桥接/MCP-style 状态快照。 |
| `sandbox-timeout-test` | 沙箱超时测试。 |
| `sandbox-write-test` | 沙箱写入限制测试。 |

Skill 脚本统一协议：

- 输入：`--payload-json`
- 输出：stdout 中打印单个 JSON 对象
- 错误：异常、stderr 或非 0 退出码由沙箱收敛为执行失败

## 17. 部署目录 `deploy/`

```text
deploy/
├── README.md
├── frp/
└── nginx/
```

| 文件 | 说明 |
|---|---|
| `deploy/README.md` | 部署说明。 |
| `deploy/frp/smart-trip-frpc.toml` | FRP 客户端配置。 |
| `deploy/nginx/default.conf` | Nginx 默认配置。 |
| `deploy/nginx/smart-trip-local-18081.conf` | 本地 18081 端口 Nginx 配置。 |
| `deploy/nginx/start-smart-trip-nginx-18081.sh` | 启动本地 Nginx 代理。 |
| `deploy/nginx/stop-smart-trip-nginx-18081.sh` | 停止本地 Nginx 代理。 |

## 18. 生成物和缓存目录

| 目录/文件 | 说明 |
|---|---|
| `frontend/node_modules/` | npm 依赖目录，可重新安装。 |
| `frontend/dist/` | 前端构建产物，可重新生成。 |
| `backend/app/**/__pycache__/` | Python 字节码缓存，可删除。 |
| `backend/scripts/__pycache__/` | Python 脚本缓存，可删除。 |
| `backend/tests/**/__pycache__/` | 测试缓存，可删除。 |
| `backend/.pytest_cache/` | pytest 缓存，可删除。 |
| `skills/public/*/scripts/__pycache__/` | Skill 脚本字节码缓存，可删除。 |
| `.idea/` | IDE 本地配置，不影响服务运行。 |
| `.git/` | Git 版本控制目录。 |

## 19. 一次请求的主要流转关系

复杂旅行规划请求的大致链路：

```text
frontend/src/App.vue
-> frontend/src/lib/api.js
-> backend/app/api/routes.py
-> backend/app/orchestration/planning/planner_service.py
-> backend/app/orchestration/planning/task_router.py
-> backend/app/orchestration/planning/slot_extractor.py
-> backend/app/orchestration/context/session_context_service.py
-> backend/app/orchestration/memory/*
-> backend/app/knowledge/retrieval/retrieval_service.py
-> backend/app/knowledge/rag/*
-> backend/app/capabilities/skills/*
-> skills/public/*/scripts/run.py
-> backend/app/orchestration/agents/*
-> backend/app/orchestration/workflow/reviewer.py
-> backend/app/orchestration/context/context_assembler.py
-> backend/app/capabilities/llm/openai_client.py
-> backend/app/capabilities/presentation/geo_service.py
-> backend/app/capabilities/image_generation/service.py
-> backend/app/core/db/repositories.py
-> frontend/src/components/ResultPanel.vue
```

简单咨询请求会经过任务路由后走轻量链路，可能只触发天气、景点等 Skill，不进入完整 RAG、多 Agent 和路线规划。

## 20. 本地启动命令

后端：

```bash
cd /home/obiah/Desktop/smart-trip/backend
python -m uvicorn app.main:app --reload --port 8001
```

前端：

```bash
cd /home/obiah/Desktop/smart-trip/frontend
npm install
npm run dev
```

前端构建：

```bash
cd /home/obiah/Desktop/smart-trip/frontend
npm run build
```

## 21. 重构后的维护规则

- 新增 HTTP 接口放 `backend/app/api/`。
- 新增配置、路径、数据库、schema 或静态媒体基础设施放 `backend/app/core/`。
- 新增规划链路、任务路由、状态恢复、Memory、Context、Workflow、Agent 放 `backend/app/orchestration/`。
- 新增 RAG、Retrieval、知识图谱能力放 `backend/app/knowledge/`。
- 新增模型调用、工具调用、外部集成、图片生成、地图展示、本地能力引擎放 `backend/app/capabilities/`。
- 新增业务 Skill 时，放在 `skills/public/<skill-name>/`，同时提供 `SKILL.md` 和 `scripts/run.py`。
- 避免在业务代码中使用 `Path(__file__).parents[n]` 推导根目录，应优先使用 `app.core.paths`。
- 不要提交真实 API Key、生成图片大文件、SQLite 运行库、`node_modules`、`dist` 或缓存目录。
